#include<stdio.h>

int main(){
    int a;
    printf("the value is a :\n");
    scanf("%d",&a); //  ('&' ampercentage used for declare)
    (a<=5) ? printf("a is greater than or equal to 5\n") : printf("a is less than 5\n");
    return 0;
}


//when you if else if or ternery condition look condition 
//'>=' in first comment will more or greater then  
//second statement used less that number given in condition 
