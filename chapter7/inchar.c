#include<stdio.h>

int main(){
    char names[3]={'A','B','C'};//a[] variAble  wher we can put multiple value
    //when character come [] in this how much value should print
    
    printf("the value of a[0] is %c \n",names[0]); //see clear ful a[] 
    //you shuld put 0,1,2, value that value will assign to given by u
    printf("the value of a[1] is %c \n",names[1]);
    printf("the value of a[2] is %c \n",names[2]);
    return 0;
}