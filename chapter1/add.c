#include <stdio.h>

int main()
{
    int a = 85,c;
    float b = 86.265;
    char d = 'k';
    int e=556 ,g=989;

    printf("the addition of two no %f \n", a+b );
    printf("the sub of two no %f \n",a-b );
    printf("the diviition of two no %f \n", b / a );
    printf("the mul of two no %f \n", a * b );
    printf("the show character %c \n", d );
    printf("extra %d \n", e + g);
}