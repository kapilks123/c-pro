#include<stdio.h>

int main(){
    char s[39];
     printf("Enter the your name: ");
     gets(s);//for no limitation like scanf
     puts(s);//just like printf  not adding the extra youn added in printf
    return 0;
}